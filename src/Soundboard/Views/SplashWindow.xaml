<Window x:Class="Soundboard.Views.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Sonar"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight">
    <Border Background="{DynamicResource SurfaceBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            Padding="40,32">
        <StackPanel HorizontalAlignment="Center">
            <Rectangle Width="28" Height="28" Fill="{DynamicResource AccentBrush}" HorizontalAlignment="Center" Margin="0,0,0,14"/>
            <TextBlock Text="SONAR" FontFamily="{DynamicResource BrandFontFamily}" FontSize="20" HorizontalAlignment="Center"
                       Foreground="{DynamicResource TextPrimaryBrush}"/>
            <TextBlock Text="STARTING UP…" FontFamily="{DynamicResource MonoFontFamily}" FontSize="11" Opacity="0.6" HorizontalAlignment="Center" Margin="0,8,0,0"
                       Foreground="{DynamicResource TextPrimaryBrush}"/>
        </StackPanel>
    </Border>
</Window>
