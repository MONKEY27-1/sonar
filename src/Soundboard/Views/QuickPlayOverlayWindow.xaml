<Window x:Class="Soundboard.Views.QuickPlayOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Soundboard.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Sonar Quick Play"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="Height"
        Width="300">
    <Border Background="{DynamicResource SurfaceBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="10"
            Padding="0,0,0,8">
        <Border.Effect>
            <DropShadowEffect Color="#000000" Opacity="0.35" BlurRadius="18" ShadowDepth="4"/>
        </Border.Effect>
        <StackPanel>
            <TextBox x:Name="SearchBox"
                     Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                     Margin="10,10,10,6"
                     Padding="8,6"
                     FontSize="13"/>

            <ScrollViewer MaxHeight="360" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Sounds}" Margin="6,0,6,0">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:SoundButtonViewModel}">
                            <Button Content="{Binding DisplayName}"
                                    HorizontalContentAlignment="Left"
                                    Padding="10,7"
                                    Margin="0,1"
                                    Command="{Binding DataContext.PlayAndDismissCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <TextBlock Text="No sounds found" HorizontalAlignment="Center" Margin="0,12,0,12"
                       Opacity="0.6" FontSize="12"
                       Visibility="{Binding HasNoResults, Converter={StaticResource BoolToVisibility}}"/>
        </StackPanel>
    </Border>
</Window>
